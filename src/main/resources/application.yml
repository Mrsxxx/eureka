eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/
    # 注册中心server服务 不需要自己作为客户端
    register-with-eureka: false
spring:
  application:
    name: eureka
server:
  port: 8761